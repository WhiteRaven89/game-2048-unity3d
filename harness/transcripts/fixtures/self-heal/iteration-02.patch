diff --git a/core/src/Game2048.Core/Rules.cs b/core/src/Game2048.Core/Rules.cs
--- a/core/src/Game2048.Core/Rules.cs
+++ b/core/src/Game2048.Core/Rules.cs
@@ -67,7 +67,9 @@
                         // tile at one merge per move: [2,2,2,2] gives [4,4,0,0], never
                         // [8,0,0,0]. Because the scan runs from the wall outward, the
                         // pair nearest the wall merges first - merges resolve in the
-                        // direction of travel.
+                        // direction of travel. Advancing only to the partner instead
+                        // of past it would let the tile this merge just produced merge
+                        // again on the next pass, which is what makes it "+ 1".
                         read = partner + 1;
                     }
                     else
